{
  "routes": [
    {
      "method": "GET",
      "path": "/carts",
      "handler": "cart.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/carts/:id",
      "handler": "cart.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/carts",
      "handler": "cart.create",
      "config": {
        "policies": []
      }
    }
  ]
}
